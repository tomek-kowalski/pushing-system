import headerStyle from '../styles/Header.module.css'

export default function Header() {

    return (
        <header>
            <h1 className={headerStyle.headerTitle} >System montoringu płatności</h1>
        </header>
    );
}